apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-analyzer-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: go-analyzer.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-analyzer
                port:
                  number: 80
